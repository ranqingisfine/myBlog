---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2026-03-17
category:
  - LeetCode 
tag:
  - 矩阵
star: true 
ticky: true

---

# 8.4.1 有效的数独

**题目**：请你判断一个 `9 x 9` 的数独是否有效。只需要 **根据以下规则** ，验证已经填入的数字是否有效即可。

1. 数字 `1-9` 在每一行只能出现一次。
2. 数字 `1-9` 在每一列只能出现一次。
3. 数字 `1-9` 在每一个以粗实线分隔的 `3x3` 宫内只能出现一次。（请参考示例图）

注意：

- 一个有效的数独（部分已被填充）不一定是可解的。
- 只需要根据以上规则，验证已经填入的数字是否有效即可。
- 空白格用 `'.'` 表示。



**分析**：

```
row[i][index]：表示第i行中的数字（index+1）出现的次数
col[j][index]：表示第j列中数字（index+1）出现的次数
subboxes[i/3][j/3][index]：表示3x3宫格中数字（index+1）出现的次数
```



**代码**：

```java
class Solution {
    public boolean isValidSudoku(char[][] board) {
        //每行 数值 出现次数
        int[][] row=new int[9][9];
        //每列 数值 出现次数
        int[][] col=new int[9][9];
        //每行 每列 数值 出现次数
        int[][][] subboxes=new int[3][3][9];
        //遍历二维数组每一个元素
        for (int i = 0; i < 9; i++) {
            for (int j = 0; j < 9; j++) {
                char c=board[i][j];
                if (c!='.'){
                    int index=c-'0'-1;
                    row[i][index]++;
                    col[j][index]++;
                    subboxes[i/3][j/3][index]++;
                    if (row[i][index] >1 || col[j][index] >1 || subboxes[i/3][j/3][index]>1){
                        return false;
                    }
                }
            }
        }
        return true;
    }
}
```

