---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2026-03-17
category:
  - LeetCode 
tag:
  - 矩阵
star: true 
ticky: true

---

# 8.4.5 生命游戏

leetCode.289

**题目**：

根据 [百度百科](https://baike.baidu.com/item/生命游戏/2926434?fr=aladdin) ， **生命游戏** ，简称为 **生命** ，是英国数学家约翰·何顿·康威在 1970 年发明的细胞自动机。

给定一个包含 `m × n` 个格子的面板，每一个格子都可以看成是一个细胞。每个细胞都具有一个初始状态： `1` 即为 **活细胞** （live），或 `0` 即为 **死细胞** （dead）。每个细胞与其八个相邻位置（水平，垂直，对角线）的细胞都遵循以下四条生存定律：

1. 如果活细胞周围八个位置的活细胞数少于两个，则该位置活细胞死亡；
2. 如果活细胞周围八个位置有两个或三个活细胞，则该位置活细胞仍然存活；
3. 如果活细胞周围八个位置有超过三个活细胞，则该位置活细胞死亡；
4. 如果死细胞周围正好有三个活细胞，则该位置死细胞复活；

下一个状态是通过将上述规则同时应用于当前状态下的每个细胞所形成的，其中细胞的出生和死亡是 **同时** 发生的。给你 `m x n` 网格面板 `board` 的当前状态，返回下一个状态。

给定当前 `board` 的状态，**更新** `board` 到下一个状态。

**注意** 你不需要返回任何东西。



**分析**：

**“先复制原始状态，再基于副本统计邻居，最后更新原数组”**：

1. 复制原矩阵到 `copyBoard`（避免更新原数组时覆盖原始状态，导致后续统计错误）；
2. 遍历每个细胞，基于 `copyBoard` 统计其 8 个邻居中的活细胞数；
3. 根据规则，用统计结果更新原数组 board的细胞状态；



**代码**：

```java
class Solution {
    public void gameOfLife(int[][] board) {
         //1、复制原数组
        int rows=board.length;
        int cols=board[0].length;
        int[][]copyBoard=new int[rows][cols];
        for(int row=0;row<rows;row++){
            for(int col=0;col<cols;col++){
                copyBoard[row][col]=board[row][col];
            }
        }
        int[]neighbors={0,1,-1};
        //2、遍历面板的每一个细胞
        for(int row=0;row<rows;row++){
            for(int col=0;col<cols;col++){
                //每个细胞周围存活的细胞数å
                int liveNeighbors=0;
                for(int i=0;i<3;i++){
                    for(int j=0;j<3;j++){
                        if(!(neighbors[i]==0&&neighbors[j]==0)){
                            int r=row+neighbors[i];
                            int c=col+neighbors[j];
                            if((r<rows&&r>=0)&&(c<cols&&c>=0)&&copyBoard[r][c]==1){
                                liveNeighbors++;
                            }
                        }
                    }
                }
                //活细胞周围活细胞数小于2或者大于3 --> 活死
                if((copyBoard[row][col]==1)&&(liveNeighbors<2||liveNeighbors>3)){
                    board[row][col]=0;
                }
                //死细胞周围活细胞数等于3 --死活
                if(copyBoard[row][col]==0&&liveNeighbors==3){
                    board[row][col]=1;
                }

            }
        }
    }
}
```

