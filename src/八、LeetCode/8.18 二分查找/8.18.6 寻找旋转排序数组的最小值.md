---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2O26-O4-12
category:
  - LeetCode 
tag:
  - 二分查找
star: true 
ticky: true




---

# 8.18.6 寻找旋转排序数组的最小值

leetcode.153

## 1、题目

已知一个长度为 `n` 的数组，预先按照升序排列，经由 `1` 到 `n` 次 **旋转** 后，得到输入数组。例如，原数组 `nums = [0,1,2,4,5,6,7]` 在变化后可能得到：

- 若旋转 `4` 次，则可以得到 `[4,5,6,7,0,1,2]`
- 若旋转 `7` 次，则可以得到 `[0,1,2,4,5,6,7]`

注意，数组 `[a[0], a[1], a[2], ..., a[n-1]]` **旋转一次** 的结果为数组 `[a[n-1], a[0], a[1], a[2], ..., a[n-2]]` 。

给你一个元素值 **互不相同** 的数组 `nums` ，它原来是一个升序排列的数组，并按上述情形进行了多次旋转。请你找出并返回数组中的 **最小元素** 。

你必须设计一个时间复杂度为 `O(log n)` 的算法解决此问题。

**示例 1：**

```
输入：nums = [3,4,5,1,2]
输出：1
解释：原数组为 [1,2,3,4,5] ，旋转 3 次得到输入数组。
```

说白了就是找数组里面的最小元素



## 2、分析

二分法划分后，必定一边有序，一边无序，最小值在无序的那边

```
例如：4 5 6 7 0 1 2
     0 1 2 3 4 5 6
第一次：left=0，right=6，mid=3，nums[mid]>nums[right]，所以0～mid有序，mid+1~right无序，最小值在无序那边，因此left=mid+1=4
第二次：left=4，right=6，mid=5，nums[mid]<nums[right]，所以右边有序，左边无序，因此right=mid=5
第三次：left=4，right=5，mid=4，nums[mid]=nums[right]，要缩右边才能找到最小值，因此right=mid=4
当left=right=4时，结束循环，返回nums[left]即是数组的最小值

例如：2 1
     0 1
第一次：left=0，right=1，mid=0，nums[mid]>nums[right]，所以右边无序，最小值在右边，因此left=mid+1=1
第二次：left=right=1，结束循环
```



## 3、代码

```java
class Solution {
    public int findMin(int[] nums) {
        int left=0;
        int right=nums.length-1;
        while (left<right){
            int mid=left+(right-left)/2;
            //最小值在右边，最小值不包括nums[mid]，所以left=mid+1
            if (nums[mid] > nums[right]){
                left=mid+1;             
            }else{
              //最小值在左边，肯能会包括nums[mid]，所以left=mid
                right=mid;
            }
        }
        return nums[left];
    }
}
```







