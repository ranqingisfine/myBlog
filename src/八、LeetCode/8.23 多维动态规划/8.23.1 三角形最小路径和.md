---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2O26-O4-22
category:
  - LeetCode 
tag:
  - 多维动态规划
star: true 
ticky: true



---

# 8.23.1 三角形最小路径和

Leetcode.120

## 1、题目

给定一个三角形 `triangle` ，找出自顶向下的最小路径和。

每一步只能移动到下一行中相邻的结点上。**相邻的结点** 在这里指的是 **下标** 与 **上一层结点下标** 相同或者等于 **上一层结点下标 + 1** 的两个结点。也就是说，如果正位于当前行的下标 `i` ，那么下一步可以移动到下一行的下标 `i` 或 `i + 1` 。

 **示例 1：**

```
输入：triangle = [[2],[3,4],[6,5,7],[4,1,8,3]]
输出：11
解释：如下面简图所示：
   2
  3 4
 6 5 7
4 1 8 3
自顶向下的最小路径和为 11（即，2 + 3 + 5 + 1 = 11）。
```

简单说：

- 三角形是一层层的，每层数字个数 = 层数
- 从最上面走到最下面，每次只能走**正下方**或**右下方**
- 求所有路径里，**数字和最小**的那条



## 2、分析

核心思想：

1. 从**倒数第二层**开始，往上算
2. 每个位置的值 = 当前值 + 下一行**相邻两个数的最小值**
3. 一路算到最顶端，顶端的值就是答案！

例如：

原三角形：

```
层0:    [2]
层1:   [3,4]
层2:  [6,5,7]
层3: [4,1,8,3]
```

**第 1 步：从层 2（倒数第二层）开始算**

层 2 每个数 = 自己 + 下一行相邻两个数的最小值

- 6：下一行相邻 4 和 1 → min=1 → 6+1=7
- 5：下一行相邻 1 和 8 → min=1 → 5+1=6
- 7：下一行相邻 8 和 3 → min=3 → 7+3=10

现在三角形变成：

```
层0:    [2]
层1:   [3,4]
层2:  [7,6,10]
层3: [4,1,8,3]
```

**第 2 步：算层 1**

- 3：下一行相邻 7 和 6 → min=6 → 3+6=9

- 4：下一行相邻 6 和 10 → min=6 → 4+6=10

现在：

```
层0:    [2]
层1:  [9,10]
层2: [7,6,10]
```

**第 3 步：算层0（顶端）**

- 2：下一行相邻 9 和 10 → min=9 → 2+9=11

最终顶端是 **11**，就是答案！



## 3、代码

```java
import java.util.List;

class Solution {
    public int minimumTotal(List<List<Integer>> triangle) {
        // 思路：自底向上动态规划
        // 从倒数第二层开始往上遍历（i 从 倒数第二层 到 0）
        for (int i = triangle.size() - 2; i >= 0; i--) {
            // 遍历当前层的每一个数字
            for (int j = 0; j < triangle.get(i).size(); j++) {
                // 当前数字 = 自己 + 下一行相邻两个数的最小值
                // 下一行相邻：j（正下方） 和 j+1（右下方）
                int min = Math.min(triangle.get(i + 1).get(j), triangle.get(i + 1).get(j + 1));
                // 更新当前位置的值
                triangle.get(i).set(j, triangle.get(i).get(j) + min);
            }
        }
        // 最顶端就是最小路径和
        return triangle.get(0).get(0);
    }
}
```









