---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2026-03-18
category:
  - LeetCode 
tag:
  - 哈希表
star: true 
ticky: true

---

# 8.5.4 有效的字母异位词

leetCode.242

**题目**：

给定两个字符串 `s` 和 `t` ，编写一个函数来判断 `t` 是否是 `s` 的 字母异位词。

字母异位词：字母异位词是通过重新排列不同单词或短语的字母而形成的单词或短语，并使用所有原字母一次。

```
示例 1:
输入: s = "anagram", t = "nagaram"
输出: true

示例 2:
输入: s = "rat", t = "car"
输出: false
```



**分析**：

解题思路同：[8.5.1 赎金信](https://ranqingisfine.github.io/myBlog/%E5%85%AB%E3%80%81LeetCode/8.5%20%E5%93%88%E5%B8%8C%E8%A1%A8/8.5.1%20%E8%B5%8E%E9%87%91%E4%BF%A1.html)

```
- 将s字符存放到map里面
- 遍历t字符，map.put(t.charAt(i),map.getOrDefalut(t.charAt(i),0)-1)
- 判断map.get(t.charAt(i))<0，则返回false
```



**代码**：

```java
class Solution {
    public boolean isAnagram(String s, String t) {
      	//判断长度！！！
        if (t.length() != s.length()){
            return false;
        }
        Map<Character,Integer> map=new HashMap<>();
        for (int i = 0; i < s.length(); i++) {
            map.put(s.charAt(i),map.getOrDefault(s.charAt(i),0)+1);
        }
        for (int i = 0; i < t.length(); i++) {
            map.put(t.charAt(i),map.getOrDefault(t.charAt(i),0)-1);
            if (map.get(t.charAt(i)) < 0 ){
                return false;
            }
        }
        return true;
    }
}
```









