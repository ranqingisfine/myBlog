---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2025-03-19
category:
  - LeetCode 
tag:
  - 哈希表
star: true 
ticky: true

---

# 8.5.9 最长连续序列

leetCode.128

**题目**：

给定一个未排序的整数数组 `nums` ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。

请你设计并实现时间复杂度为 `O(n)` 的算法解决此问题。

```
示例 1：
输入：nums = [100,4,200,1,3,2]
输出：4
解释：最长数字连续序列是 [1, 2, 3, 4]。它的长度为 4。
```



**分析**：

- 所有元素放到set里面
- 访问每个元素，如果元素-1在set里面不存在，则代表此处可以作为起点
- 循环判断，记录长度，和历史长度取最大值



**代码**：

```java
class Solution {
    public int longestConsecutive(int[] nums) {
        Set<Integer> set=new HashSet<>();
        for (int num : nums) {
            set.add(num);
        }
        int maxLen=0;
      //注意，要是遍历nums的话重复元素的情况下会超时，因此要遍历去重后的集合
        for (Integer num : set) {
            if (!set.contains(num-1)){
                int len=1;
                while(set.contains(++num)){
                    len++;
                }
                maxLen=Math.max(maxLen,len);
            }
        }
        return maxLen;
    }
}
```









