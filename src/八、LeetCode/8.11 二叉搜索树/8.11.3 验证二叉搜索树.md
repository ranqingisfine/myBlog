---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2026-03-26
category:
  - LeetCode 
tag:
  - 二叉搜索树
star: true 
ticky: true






---

# 8.11.3 验证二叉搜索树

leetcode.98

**题目**：

给你一个二叉树的根节点 `root` ，判断其是否是一个有效的二叉搜索树。

有效 二叉搜索树定义如下：

- 节点的左子树只包含 严格小于 当前节点的数。
- 节点的右子树只包含 严格大于 当前节点的数。
- 所有左子树和右子树自身必须也是二叉搜索树。

**分析**：

二叉搜索树+中序遍历=递增数组

这里要注意的是，二叉搜索树中序遍历后的数组相邻元素不能相等，也就是说如果有相邻元素相等，则返回false

例如[2,2,2]，这种



**代码**：

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
 List<Integer> list=new ArrayList<>();

    public boolean isValidBST(TreeNode root) {
        isValidCal(root);
        if (list.size() == 1){
            return true;
        }
        for (int i = 1; i < list.size(); i++) {
            //注意，左边元素只要 大于等于右边元素，则返回false，注意要有等于这个条件
            if (list.get(i-1) >= list.get(i)){
                return false;
            }
        }
        return true;
    }
    public void isValidCal(TreeNode root){
        if (root == null){
            return ;
        }
        isValidCal(root.left);

        list.add(root.val);

        isValidCal(root.right);
    }
}
```

