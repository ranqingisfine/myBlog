---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2026-03-20
category:
  - LeetCode 
tag:
  - 栈
star: true 
ticky: true




---

# 8.7.3 最小栈

leetCode.155

**题目**：

设计一个支持 `push` ，`pop` ，`top` 操作，并能在常数时间内检索到最小元素的栈。

实现 `MinStack` 类:

- `MinStack()` 初始化堆栈对象。
- `void push(int val)` 将元素val推入堆栈。
- `void pop()` 删除堆栈顶部的元素。
- `int top()` 获取堆栈顶部的元素。
- `int getMin()` 获取堆栈中的最小元素。



**分析**：

定义栈和最小栈来实现



**代码**：

```java
class MinStack {
    // 主栈：存储所有元素
    private Deque<Integer> mainStack;
    // 辅助栈：同步存储每个阶段的最小值
    private Deque<Integer> minStack;

    // 初始化栈
    public MinStack() {
        mainStack = new ArrayDeque<>();
        minStack = new ArrayDeque<>();
        // 辅助栈初始化一个极大值，避免空栈判断（可选）
        minStack.push(Integer.MAX_VALUE);
    }

    // 压入元素
    public void push(int val) {
        mainStack.push(val);
        // 辅助栈压入「当前最小值」（val 和辅助栈顶的较小值）
        int currentMin = Math.min(val, minStack.peek());
        minStack.push(currentMin);
    }

    // 弹出元素
    public void pop() {
        // 主栈和辅助栈同步弹出
        mainStack.pop();
        minStack.pop();
    }

    // 获取栈顶元素
    public int top() {
        return mainStack.peek();
    }

    // 获取最小值（O(1)）
    public int getMin() {
        return minStack.peek();
    }
}
```

