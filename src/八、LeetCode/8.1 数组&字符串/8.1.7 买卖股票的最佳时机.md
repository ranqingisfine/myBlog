---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2026-02-24
category:
  - LeetCode 
tag:
  - 贪心 
star: true 
ticky: true



---

# 8.1.7 买卖股票的最佳时机

leetCode.121

**题目**：给定一个数组 `prices` ，它的第 `i` 个元素 `prices[i]` 表示一支给定股票第 `i` 天的价格。你只能选择 **某一天** 买入这只股票，并选择在 **未来的某一个不同的日子** 卖出该股票。设计一个算法来计算你所能获取的最大利润。返回你可以从这笔交易中获取的最大利润。如果你不能获取任何利润，返回 `0` 。

**分析**：第i天卖出股票的最大利润=第i天的股价-前面天数中最小的股价，然后与已知的最大利润比较，如果大于则更新当前最大利润的值。

**代码**：

```java
class Solution {
    // 思路：每天卖出的价格 - 历史最低价 = 当天最大获利
    public int maxProfit(int[] prices) {
        // 记录全局最大利润，初始为0（不交易则利润为0）
        int maxProfit=0;
        // 记录遍历过程中出现的历史最低价格，初始化为整数最大值
        int minPrice=Integer.MAX_VALUE;
        
        // 遍历每一天的股票价格
        for (int i = 0; i < prices.length; i++) {
            // 更新历史最低价：取之前的最低价 和 当前价格 中更小的那个
            minPrice=Math.min(minPrice,prices[i]);
            // 计算当前卖出的利润，更新最大利润
            maxProfit=Math.max(prices[i]-minPrice,maxProfit);
        }
        
        // 返回最终的最大利润
        return maxProfit;
    }
}
```















