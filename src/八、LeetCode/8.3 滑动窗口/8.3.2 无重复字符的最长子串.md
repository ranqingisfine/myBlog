---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2025-03-13
category:
  - LeetCode 
tag:
  - 滑动窗口
star: true 
ticky: true


---

# 8.3.2 无重复字符的最长子串

leetCode.3

**题目**：给定一个字符串 `s` ，请你找出其中不含有重复字符的 **最长 子串** 的长度。

**分析**：滑动窗口，哈希表

**代码**：

```java
class Solution {
    public int lengthOfLongestSubstring(String s) {
        // 边界处理：空字符串直接返回0
        if (s.length()==0) return 0;
        // 哈希表：记录每个字符最后一次出现的下标
        HashMap<Character, Integer> map = new HashMap<Character, Integer>();
        int max = 0; // 记录最长无重复子串的长度
        int left = 0; // 滑动窗口左边界
        
        // i是滑动窗口右边界，遍历整个字符串
        for(int i = 0; i < s.length(); i ++){
            // 关键：当前字符已在哈希表中（说明重复了）
            if(map.containsKey(s.charAt(i))){
                // 左边界移动到「重复字符的下一个位置」，且取max避免left回退（比如abba的情况）
                left = Math.max(left,map.get(s.charAt(i)) + 1);
            }
            // 更新当前字符的最新下标（无论是否重复，都要更新）
            map.put(s.charAt(i),i);
            // 计算当前窗口长度（i-left+1），更新max
            max = Math.max(max,i-left+1);
        }
        return max;
    }
}
```

