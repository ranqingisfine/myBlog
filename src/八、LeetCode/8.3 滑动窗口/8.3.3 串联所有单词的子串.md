---
<!--cover: /assets/images/cover2.jpg-->
icon: pen-to-square
date: 2025-03-16
category:
  - LeetCode 
tag:
  - 滑动窗口
star: true 
ticky: true



---

# 8.3.3 串联所有单词的子串

**题目**：给定一个字符串 `s` 和一个字符串数组 `words`**。** `words` 中所有字符串 **长度相同**。

 `s` 中的 **串联子串** 是指一个包含 `words` 中所有字符串以任意顺序排列连接起来的子串。

- 例如，如果 `words = ["ab","cd","ef"]`， 那么 `"abcdef"`， `"abefcd"`，`"cdabef"`， `"cdefab"`，`"efabcd"`， 和 `"efcdab"` 都是串联子串。 `"acdbef"` 不是串联子串，因为他不是任何 `words` 排列的连接。

返回所有串联子串在 `s` 中的开始索引。你可以以 **任意顺序** 返回答案。



**分析**：

使用滑动窗口+哈希表

```
输入：s = "barfoothefoobarman", words = ["foo","bar"]
输出：[0,9]
```

1、字符串s有words[0].length()中划分，不同的划分产生的单词不一样

```
start=0时bar/foo/the/foo/bar/man
start=1时b/arf/oot/hef/oob/arm/an
start=2时ba/rfo/oth/efo/oba/rma/n
```

2、定义一个滑动窗口windowMap和字典wordMap，key=单词，如果两个map一样，则记录子串的起始位置。

关键在于如何判断windowMap和wordMap相等。

- 如果单词在wordMap里，则将单词添加到windowMap，并记录windowMap中单词个数count
- 如果windowMap单词出现次数超过wordMap，则窗口左边界右移
- 如果窗口长度等于单词数时，意味着找到了结果，将下标放入结果集，窗口左边界右移

一句话概括：windowMap和wordMap相当的条件是key值相等、单词个数相等（若windowMap单词个数大于wordMap，则左边界右移）、总单词数相等



**代码**：

```java
class Solution {
    public static List<Integer> findSubstring(String s, String[] words) {
        int wordLen = words[0].length();
        int numWords = words.length;
        int windowLen = wordLen * numWords;
        if (windowLen > s.length()) return new ArrayList<>();

        Map<String, Integer> wordMap = new HashMap<>();
        for (String word : words) {
            wordMap.put(word, wordMap.getOrDefault(word, 0) + 1);
        }

        List<Integer> resList = new ArrayList<>();
        // 分别从 wordLen 个不同起点开始滑动
        for (int i = 0; i < wordLen; i++) {
            slide(s, i, wordLen, numWords, wordMap, resList);
        }
        return resList;
    }

    private static void slide(String s, int start, int wordLen, int numWords, Map<String, Integer> wordMap, List<Integer> resList) {
        Map<String, Integer> windowMap = new HashMap<>();
        int left = start, count = 0;

        for (int right = start; right + wordLen <= s.length(); right += wordLen) {
            String word = s.substring(right, right + wordLen);
            if (wordMap.containsKey(word)) {
                windowMap.put(word, windowMap.getOrDefault(word, 0) + 1);
                count++;

                // 如果当前单词出现次数超过目标次数，左边界右移
                while (windowMap.get(word) > wordMap.get(word)) {
                    String leftWord = s.substring(left, left + wordLen);
                    windowMap.put(leftWord, windowMap.get(leftWord) - 1);
                    left += wordLen;
                    count--;
                }

                // 当窗口长度等于总单词数时，找到结果
                if (count == numWords) {
                    resList.add(left);
                    String leftWord = s.substring(left, left + wordLen);
                    windowMap.put(leftWord, windowMap.get(leftWord) - 1);
                    left += wordLen;
                    count--;
                }
            } else {
                // 不在 words 中，窗口重置
                windowMap.clear();
                count = 0;
                left = right + wordLen;
            }
        }
    }
}
```

